#liModal.modal.fade(tabindex='-1', role='dialog', aria-labelledby='loginModalLabel',  ng-blur='vm.liClear()')
  .modal-dialog(role='document')
    .modal-content
      form(novalidate, name='liForm', ng-submit='vm.login()')
        .modal-header
          button.close(type='button', data-dismiss='modal', aria-label='Close')
            span(aria-hidden='true') ×
          h4#liModalLabel.modal-title Login
        .modal-body
          div
            label(for='username') Username
            input#liUsername(type='text', name='username', ng-model='vm.liUsername', required, autocomplete='off')
          br
          div
            label(for='password') Password
            input#liPassword(type='password', name='password', ng-model='vm.liPassword', required)
          p(ng-hide='vm.liError === ""') {{vm.liError}}
        .modal-footer
          div
            input(type='submit', value='Login', ng-disabled='liForm.$invalid')
